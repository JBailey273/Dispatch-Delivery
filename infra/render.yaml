# Canonical copy of the Render Blueprint for infra-focused workflows.
# Keep this file in sync with the repository-root `render.yaml`.
services:
  - type: web
    name: dispatch-web
    env: node
    rootDir: apps/web
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NEXT_PUBLIC_API_BASE_URL
        value: https://dispatch-api.onrender.com/api/v1

  - type: web
    name: dispatch-api
    env: python
    rootDir: apps/api
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: DATABASE_URL
        fromDatabase:
          name: dispatch-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: dispatch-redis
          type: redis
          property: connectionString
      - key: JWT_SECRET
        sync: false
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_FROM_NUMBER
        sync: false
      - key: R2_ENDPOINT_URL
        sync: false
      - key: R2_ACCESS_KEY_ID
        sync: false
      - key: R2_SECRET_ACCESS_KEY
        sync: false
      - key: R2_BUCKET
        sync: false
      - key: CHANNEL_API_KEYS_JSON
        sync: false

  - type: worker
    name: dispatch-worker
    env: python
    rootDir: apps/worker
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python -m worker.main
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: DATABASE_URL
        fromDatabase:
          name: dispatch-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: dispatch-redis
          type: redis
          property: connectionString
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_FROM_NUMBER
        sync: false

  - type: redis
    name: dispatch-redis
    ipAllowList: []
    plan: starter

databases:
  - name: dispatch-postgres
    databaseName: dispatch
    user: dispatch
    plan: basic-256mb
